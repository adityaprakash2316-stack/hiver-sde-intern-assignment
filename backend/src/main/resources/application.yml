server:
  port: 8080

ai:
  service:
    base-url: http://localhost:8000

spring:
  application:
    name: hiver-support-agent

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.hiver: INFO
